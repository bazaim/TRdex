// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
	production: false,
	firebaseConfig: {
		apiKey: 'AIzaSyC16d7V9KLdTo7iWw01tn0iIywMfbn-uUY',
		authDomain: 'trdex-dev.firebaseapp.com',
		databaseURL: 'https://trdex-dev.firebaseio.com',
		projectId: 'trdex-dev',
		storageBucket: 'trdex-dev.appspot.com',
		messagingSenderId: '405491364878',
		appId: '1:405491364878:web:dccd703e77cb17832ef85f'
	}
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
